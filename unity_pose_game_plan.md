# Unityで作る「PC + カメラ + プロジェクター」姿勢連動ゲーム設計

## 1. 目的
PCに接続したカメラでプレイヤーの姿勢（ポーズ）を推定し、その結果をUnityゲームにリアルタイム反映して、
プロジェクター投影面で遊ぶ体感型ゲームを実現する。

---

## 2. 想定ゲーム例（コンセプト）
- プレイヤーの両手位置で「シールド」や「光の壁」を操作
- 体の傾きでキャラクターの回避行動を制御
- 指定ポーズ（例: 両手を上げる、しゃがむ）でスキル発動

この構成にすると、コントローラーなしで直感的に遊べる。

---

## 3. システム全体構成

### ハードウェア
- PC（Unity実行 + 姿勢推定処理）
- USB/Webカメラ（できれば60fps対応）
- プロジェクター（短焦点推奨）

### ソフトウェア
- Unity（ゲーム本体）
- 姿勢推定ライブラリ（候補: MediaPipe Pose / OpenPose / MoveNet）
- Unity連携層（UDP / WebSocket / ローカルプラグイン）

### データフロー
1. カメラ映像を取得
2. 姿勢推定で骨格キーポイント（例: 肩、肘、手首、腰、膝）を抽出
3. キーポイント座標を正規化してUnityへ送信
4. Unity側で平滑化・しきい値判定・アクション変換
5. ゲームオブジェクトと演出へ反映し、プロジェクターで投影

---

## 4. 実装の推奨アーキテクチャ

### A. 姿勢推定プロセスを分離（推奨）
- Pythonプロセス: カメラ入力 + 姿勢推定
- Unity: ゲームロジック + 描画
- 通信: UDP（低遅延）

#### メリット
- 推定モデルを差し替えやすい
- Unity本体のフレーム低下を切り分けしやすい
- デバッグ時に姿勢推定を単体検証できる

### B. Unity単体で完結
- BarracudaやプラグインでUnity内推定
- 構成はシンプルだが、導入難易度と最適化コストが上がりやすい

---

## 5. Unity側の実装ポイント

### 5.1 受信データ構造（例）
```json
{
  "timestamp": 1730000000.123,
  "keypoints": {
    "left_wrist": [0.42, 0.31, 0.88],
    "right_wrist": [0.61, 0.30, 0.91],
    "nose": [0.52, 0.18, 0.95]
  }
}
```
- 配列: `[x, y, confidence]`
- 座標は 0.0〜1.0 の正規化値

### 5.2 平滑化
- ジッター対策で `Lerp` や移動平均を適用
- 低信頼度（confidenceが低い）フレームは補間または無視

### 5.3 ジェスチャー判定例
- 両手を上げる: `left_wrist.y < nose.y && right_wrist.y < nose.y`
- しゃがむ: `hip.y` と `knee.y` の距離が閾値以下
- 左右移動: 胴体中心 `x` の移動量で判定

### 5.4 投影を前提としたUI/演出
- 遠距離視認のため高コントラスト配色
- ステータス表示は大きめフォント
- プレイヤー位置ガイドを床面想定で表示

---

## 6. キャリブレーション（重要）
1. カメラ座標系 → ゲーム座標系の対応付け
2. プレイエリアの四隅を基準に補正
3. プレイヤー身長差を吸収する正規化

### 実運用向け補正
- 開始時に「Tポーズ」や「両手上げ」で基準姿勢を取得
- そのユーザーの腕長・肩幅を基準に閾値を自動調整

---

## 7. パフォーマンス目標
- 入力遅延: 100ms以下（理想は60ms前後）
- 推定FPS: 30以上（体感ゲームの最低ライン）
- Unity描画FPS: 60目標

### 最適化のコツ
- 推定用映像解像度を下げる（例: 1280x720 → 640x360）
- 推定処理とゲーム描画を別スレッド/別プロセス化
- 送信データは必要関節のみ

---

## 8. 開発ステップ（最短ルート）
1. カメラ映像から骨格点を取得（Python + MediaPipe）
2. 骨格点をUDPでUnityに送る
3. Unityでデバッグ表示（関節点の可視化）
4. 1つのジェスチャーで1アクションを実装
5. キャリブレーション画面を追加
6. 投影環境でプレイテストし、閾値を調整

---

## 9. 失敗しやすいポイント
- 照明が暗く、推定が不安定
- 背景が複雑で誤検出が増える
- プロジェクター遅延を見積もっていない
- ジェスチャー閾値が固定で体格差に対応できない

---

## 10. 初期PoCの仕様案（1〜2週間）
- 入力: 1人プレイ、上半身のみ追跡
- 動作: 「両手上げ」でバリア発動
- 画面: 隕石が降ってきて、バリアで防ぐ
- 成果物: 実時間反映デモ + 調整可能な閾値UI

このPoCで「遅延」「誤検出率」「遊べるか」の3点を確認し、
問題なければ複数ジェスチャーとゲーム性を拡張する。
